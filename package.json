{
  "name": "promptquarantine",
  "version": "0.1.0",
  "description": "Local-first CLI for quarantining untrusted prompt content.",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "promptquarantine": "./dist/cli.js"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.js",
    "package:smoke": "npm pack --dry-run",
    "release:check": "npm test && npm run package:smoke"
  },
  "keywords": [],
  "author": "StackForge User",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
